# Core agent framework
spade==3.3.2

# Web API
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9      # for file upload parsing

# HTTP client (async) — for calling Ollama API
httpx==0.27.0

# PDF processing
pymupdf==1.24.3              # PDF → image (pip install pymupdf)
pytesseract==0.3.10          # Python wrapper for Tesseract OCR
Pillow==10.3.0               # Image handling

#    Ollama:
#    Pull the model: ollama pull llama3.2:1b
#    Start server:   ollama serve